{
  "name": "tinkerspace-calicut-digital-twin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "vite --configLoader native", "build": "vite build --configLoader native", "preview": "vite preview --configLoader native --host 0.0.0.0", "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=22.12.0" },
  "dependencies": { "three": "^0.180.0" },
  "devDependencies": { "vite": "^7.1.7" }
}
